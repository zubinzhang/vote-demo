sudo: false
language: node_js
node_js:
  - '8'
  - '10'
env:
  - NODE_ENV=develop
install:
  - npm i npminstall && npminstall
script:
  - npm run ci
after_script:
  - npminstall codecov && codecov
services:
  - redis
  - mysql
